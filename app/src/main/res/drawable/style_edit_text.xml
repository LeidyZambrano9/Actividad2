<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Color de fondo -->
    <solid android:color="@color/Letra_Boton"/>

    <!-- Bordes -->
    <stroke
        android:width="1dp"
        android:color="@color/Color_app"/>

    <!-- Esquinas redondeadas -->
    <corners android:radius="12dp"/>

    <!-- Espaciado interno -->
    <padding
        android:left="12dp"
        android:top="10dp"
        android:right="12dp"
        android:bottom="10dp"/>

</shape>